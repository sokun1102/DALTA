import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent sẽ đăng ký component chính (App) của bạn
// để Expo/React Native biết phải bắt đầu từ đâu
registerRootComponent(App);